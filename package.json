{
  "name": "tokensfun-mcp",
  "version": "1.0.0",
  "private": true,
  "description": "tokensfun mcp for Claude Code existing-app token deployment.",
  "bin": {
    "tokensfun-mcp": "./server.js"
  },
  "scripts": {
    "start": "node server.js",
    "tools": "node server.js --tools",
    "test": "node server.test.js"
  },
  "engines": {
    "node": ">=18"
  }
}
